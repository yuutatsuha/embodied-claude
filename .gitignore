# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Node.js / Bun
node_modules

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment variables (NEVER commit these!)
.env
.env.local
.env.*.local

# env-sensor firmware secrets (WiFi creds / device config live on-device only)
env-sensor-mcp/firmware/config.py

# MCP config (may contain API keys)
.mcp.json

# Captured images
/captures/
*.jpg
*.jpeg
*.png

# Testing
.pytest_cache/
.coverage
htmlcov/

# ChromaDB test data
:memory:/

# OS
.DS_Store
Thumbs.db

# Autonomous action (contains credentials and user-specific settings)
autonomous-mcp.json
autonomous-action.sh

# Autonomous action logs and session
.autonomous-logs/
.heartbeat-session-id
heartbeat-session-id
last-session-date.txt

# User-specific config (use *.sample.conf as template)
schedule.conf
desires.conf
desires.json

# Per-instance autonomous behavior notes (track last-run dates / continuation; personal scratch)
ROUTINES.md
TODO.md

# Google API credentials (NEVER commit)
scripts/credentials.json
scripts/token.json

# Tuya device scan outputs (may contain device info)
snapshot.json
tinytuya.json
tuya-raw.json
devices.json

# Reference implementations (cloned repos for study)
references/
# Reference material dropped in for study (e.g. copyrighted workbooks; private)
reference/

# Restore/sync scratch logs
.restore-uv-sync.log

# Per-person seeds for relationship-mcp (contain private facts)
.local/


# Discord image attachment cache (downloaded by discord-mcp fetch_recent_images)
.discord-attachments/

# Discord gateway runtime logs and session id (regenerated at runtime)
.discord-gateway-logs/
discord-session-id
